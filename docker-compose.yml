version: '3.9'

services:
  envoy:
    image: envoyproxy/envoy:v1.27.0
    volumes:
      - ./envoy.yaml:/etc/envoy/envoy.yaml
    ports:
      - "8080:8080"
    depends_on:
      - dummy-server

  dummy-server:
    build: ./dummy-server
    ports:
      - "8081:80"